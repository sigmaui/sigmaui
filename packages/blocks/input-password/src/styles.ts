import { StyleFn } from '@sigma-ui-kit/theme';
import { InputPasswordBaseProps, ClassKeys } from './types';

const styles: StyleFn<InputPasswordBaseProps, ClassKeys> = props => {
  const { tokens } = props;

  return {
    root: {},
  };
};

export default styles;
